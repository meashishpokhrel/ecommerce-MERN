import React from 'react'
import { Helmet } from 'react-helmet'

const Meta = ({ title, description, keywords }) => {
  return (
    <Helmet>
      <title>{title}</title>
      <meta name='description' content={description} />
      <meta name='keyword' content={keywords} />
    </Helmet>
  )
}

Meta.defaultProps = {
  title: 'Welcome To OnlineShop',
  description: 'We belive in selling most affordable products',
  keywords: 'electronics, quality electronics, exclusive electroincs',
}

export default Meta
